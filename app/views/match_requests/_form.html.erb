<%= form_for(@match_request) do |f| %>
  <% if @match_request.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@match_request.errors.count, "error") %> prohibited this match_request from being saved:</h2>

      <ul>
      <% @match_request.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :request_user_id %><br>
    <%= f.number_field :request_user_id %>
  </div>
  <div class="field">
    <%= f.label :target_user_id %><br>
    <%= f.number_field :target_user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
